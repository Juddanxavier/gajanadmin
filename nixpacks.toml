# Nixpacks Configuration for GajanAdmin
# Forced Node.js environment to prevent incorrect Deno detection

providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
